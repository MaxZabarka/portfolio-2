{
  "name": "shapes-background",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@react-three/drei": "^9.5.5",
    "@react-three/fiber": "^8.0.12",
    "@react-three/postprocessing": "^2.3.2",
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.1.1",
    "@testing-library/user-event": "^13.5.0",
    "gsap": "^3.10.4",
    "node-sass": "^7.0.1",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "5.0.1",
    "sass-loader": "^12.6.0",
    "three": "^0.139.2",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -r docs build & react-scripts build && mv build docs",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && git add . && git commit -m 'deploy' && git push"

  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
